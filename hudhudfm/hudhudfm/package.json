{
  "name": "hudhudfm-firebase-rules",
  "private": true,
  "scripts": {
    "test:rules": "mocha \"firebase/test/**/*.test.js\" --timeout 20000",
    "emulators:test": "firebase emulators:exec --only firestore,storage \"npm run test:rules\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.6.0",
    "mocha": "^11.7.5"
  }
}
